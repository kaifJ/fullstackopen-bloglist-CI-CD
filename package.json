{
  "name": "blog-list-application",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "build": "rm -fr build && cd ./bloglist-frontend && rm -rf node_modules && npm install && npm run build && cp -r build ../",
    "deploy": "flyctl deploy",
    "deploy:full": "npm run build && npm run deploy",
    "lint": "eslint .",
    "eslint": "eslint './**/*.{js,jsx}'",
    "test:e2e": "cd ./bloglist-frontend && npm run cypress:open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "lodash": "^4.17.21",
    "mongoose": "^6.7.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.27.0",
    "eslint-plugin-react": "^7.32.2",
    "jest": "^29.2.2",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.1"
  }
}
